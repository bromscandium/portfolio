# TODO / Changelog — terminal portfolio

Список усіх запланованих змін (включно з уже зробленими), щоб нічого не загубити.
Статуси: ✅ зроблено · 🟡 в процесі / потребує перевірки · ⬜ ще не роблено (обговорення)

---

## Модалка / футер / моди

- ✅ **Ребренд модів у «view»** — `developer` / `human-being` (укр: `розробник` / `людина`).
  Винесено в єдине типізоване джерело `MODE_META` у `lib/modes.ts` (`label`/`desc`/`primary`/`branch`).
  Тягнуть звідти: свічер «view» у status bar, `ProfilePicker` (рендер циклом по `MODES`), Command Palette.
  Прибрано старий розкид (`visitor`/`human`). Додати новий мод = один об'єкт у `MODE_META`.
- ✅ **Мобільний футер** — `min-h-[70vh]` → `70dvh` (не стрибає, коли ховається шторка браузера).
  🟡 перевірити на реальному девайсі ~390px.

## Консоль

- ✅ **git / git log** — звичайний лог + `--graph` (аліас `git lg`) з графом; коректні помилки
  (голий `git` / `git --help` → usage; невідома субкоманда → `See 'git --help'`).
- ✅ **Розширення git + аліаси** — `branch`/`br`, `status`/`st`, `checkout`/`co`, `commit`, `push [--force]`,
  `pull`, `fetch`, `diff`, `stash`, `reset`, `rebase`, `remote`, `config`, `blame`, `tag`.
- ✅ **git-пасхалки** + `git checkout developer` / `git checkout human-being` перемикає в'ю
  (branch = view; гілки з `MODE_META.branch`). checkout у dev-моді перекидає в human-being.
- ✅ **Пам'ять / історія / розмітка** — диспетч спец-команд по першому токену
  (`tree portfolio`, `history 10` більше не падають); дедуп послідовних дублів історії + обрізання in-memory до 100.
- ✅ **cmatrix** — matrix-дощ на canvas у тілі консолі, вихід по клавіші/кліку.
  🟡 перевірити після hard-refresh (нові файли).
- ✅ **Пасхалки** — `sudo pacman -Syu` (фейк-апгрейд), голий `pacman`, `yay`/`paru`;
  базові: `mkdir`, `touch`, `ps`, `top`/`htop`, `ping`, `curl`/`wget`.
- ⬜ **Retro / CRT-стиль консолі** — лише в консолі. Обговорити тригер (команда `crt` / тоглик)
  та ефект (scanlines + phosphor glow + flicker + vignette).

## UI

- ✅ **Arch-лого — анімація** (`components/layout/ArchLogo.tsx`):
  - hover → іконка «жирніє» з cross-fade у жирний градієнт-шимер + `scale(1.08)`;
  - через 2с → фраза `i use Arch btw` виринає **з-за іконки**, летить вправо, погойдуючись (sine, повільна хвилька);
  - unhover під час польоту → вертається за іконку;
  - удар об правий край → літери розсипаються й повільно падають вниз;
  - лежать і мигають «як моб при смерті» (~6с: повільно → швидко → зникли);
  - після удару hover ігнорується (не обривається різко).
- ⬜ **Skills — розкриття карток** по кліку (версії / під-технології).
  Пасхалка **лише в developer**: розкрита картка «оживає» — docker-логи / процеси / навантаження CPU.
  У `human-being` — прибрати hover-ефект, статичні картки. Обговорити вигляд анімації.
- 🟡 **Версії в інтро** — `next 16.2 · py 3.14.6 · docker 29.6`. Наче актуально; уточнити, чи щось міняти.

## Repo / інфра

- ✅ **deploy-скрипт** — `gh-pages -d out -t true` → `--dotfiles`.
- ✅ **EMAIL** у Contact — з `LINKS.email` (прибрано дубль).

---

## Ще не змерджено / нагадування

- Гілка `develop`. Не пушити / не форс-пушити без явної команди.
- PR #2 показує старі коміти, доки локальна історія не запушена (кредли `bromscandium`).
