import type { Combo } from '@/lib/i18n';

export const CMD = 'whoami --verbose';
export const ALL_COMBOS: Combo[] = ['dev-en', 'dev-uk', 'human-en', 'human-uk'];
export const PRE_DELAY = 1;
export const EXPAND_DELAY = 0.55;
